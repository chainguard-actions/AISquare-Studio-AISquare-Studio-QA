name: 🧪 Test AutoQA Action

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-action-components:
    name: 🔧 Test Action Components
    runs-on: ubuntu-latest
    
    steps:
      - name: 📋 Checkout Code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      
      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'
      
      # Exact-match key only, and one canonical key shared by every step that
      # touches ~/.cache/ms-playwright. Rationale in action.yml.
      - name: 🎭 Cache Playwright Browsers
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('requirements.txt') }}-v3

      - name: 📦 Install Dependencies
        run: |
          pip install -r requirements.txt
          
      # Only install browsers if cache miss (saves significant time on cache hit)
      - name: 🎭 Install Playwright Browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: |
          playwright install chromium
          playwright install-deps chromium
      
      # For cached browsers, only install system dependencies
      - name: 🎭 Setup Playwright (cached browsers)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: |
          playwright install-deps chromium
      
      # Asserts the pip package and the cached browser build actually agree, which
      # is the exact failure this workflow's cache fix addresses. It lives in THIS
      # job on purpose: no secrets are required, so unlike `test-self-autoqa` it
      # still runs on fork and Dependabot PRs — and a Dependabot bump of the
      # `playwright==` pin is precisely the change that must not merge unvalidated.
      - name: "🎭 Smoke: chromium actually launches"
        run: |
          python -c "
          from importlib.metadata import version
          from playwright.sync_api import sync_playwright
          with sync_playwright() as p:
              b = p.chromium.launch()
              page = b.new_page()
              page.set_content('<h1 data-testid=ok>ok</h1>')
              assert page.inner_text('[data-testid=ok]') == 'ok'
              b.close()
          print('✅ chromium launched under playwright ' + version('playwright'))
          "

      - name: 🔍 Test AutoQA Parser
        run: |
          python -c "
          from src.autoqa.parser import AutoQAParser
          
          parser = AutoQAParser()
          test_pr_body = '''
          ## Feature Test
          
          \`\`\`autoqa
          flow_name: feature_test
          tier: B
          \`\`\`
          1. Navigate to login page at \"/login\"
          2. Enter valid credentials
          3. Click login button
          4. Verify dashboard appears
          '''
          
          assert parser.has_autoqa_tag(test_pr_body), 'AutoQA tag detection failed'
          steps = parser.parse_test_steps(test_pr_body)
          assert len(steps) == 4, f'Expected 4 steps, got {len(steps)}'
          print('✅ AutoQA Parser tests passed')
          "

      - name: 🧠 Run Memory Tracker Tests
        run: |
          python -m pytest tests/test_memory_tracker.py -v --tb=short

      - name: 📋 Update AutoQA Memory
        run: |
          python qa_runner.py --memory-scan

  test-self-autoqa:
    name: 🚀 Test Self AutoQA
    runs-on: ubuntu-latest
    # This job needs OPENAI_API_KEY / STAGING_URL / STAGING_EMAIL / STAGING_PASSWORD.
    # GitHub does not expose repository secrets to pull_request runs from a fork, nor
    # to Dependabot-authored PRs, so the job could only ever fail there (e.g. #43).
    # Skip it in exactly those two cases; push and workflow_dispatch are unaffected.
    # Do NOT "fix" this by switching the trigger to pull_request_target — this repo
    # is PUBLIC with forks, and pull_request_target would run fork code with those
    # secrets in scope. A skipped job is the correct outcome, not a workaround.
    if: ${{ github.event_name != 'pull_request' || (github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.user.login != 'dependabot[bot]' && github.actor != 'dependabot[bot]') }}

    steps:
      - name: 📋 Checkout Code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # Same canonical key as the other two steps: all three mount the identical
      # path, and requirements.txt / .autoqa-action/requirements.txt are the same
      # file, so a shared key lets them reuse one warm cache instead of paying
      # three separate ~100MB misses. Rationale in action.yml.
      - name: 🎭 Cache Playwright Browsers (for Action)
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('requirements.txt') }}-v3

      - name: 🤖 Test AutoQA Action (Self)
        uses: ./
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          staging-url: ${{ secrets.STAGING_URL }}
          staging-email: ${{ secrets.STAGING_EMAIL }}
          staging-password: ${{ secrets.STAGING_PASSWORD }}
          target-repo-path: '.'
          git-user-name: 'AutoQA Test Bot'
          git-user-email: 'rabia.tahirr@opengrowth.com'
          action-ref: ${{ github.sha }}
          pr-body: |
            ## Test PR for AutoQA
            
            ```autoqa
            flow_name: login_test
            tier: A
            area: auth
            ```
            1. Navigate to login page at "/login"
            2. Enter valid email credentials
            3. Enter valid password
            4. Click login button
            5. Verify successful login and dashboard redirect
          test-directory: 'tests/autoQA-self-test'
      
      - name: 📊 Verify Test Generation
        run: |
          if [ -d "tests/autoQA-self-test" ]; then
            echo "✅ Test directory created"
            find tests/autoQA-self-test -type f -name '*.py' | head -20
            
            if find tests/autoQA-self-test -type f -name 'test_*.py' | grep -q .; then
              echo "✅ Test files generated"
              find tests/autoQA-self-test -type f -name 'test_*.py' -exec head -20 {} +
            else
              echo "❌ No test files found"
              exit 1
            fi
          else
            echo "❌ Test directory not created"
            exit 1
          fi

  validate-action-yml:
    name: 📋 Validate Action Definition
    runs-on: ubuntu-latest
    
    steps:
      - name: 📋 Checkout Code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      
      - name: 🔍 Validate action.yml
        run: |
          # Check required fields
          if ! grep -q "name:" action.yml; then
            echo "❌ action.yml missing 'name' field"
            exit 1
          fi
          
          if ! grep -q "description:" action.yml; then
            echo "❌ action.yml missing 'description' field"
            exit 1
          fi
          
          if ! grep -q "runs:" action.yml; then
            echo "❌ action.yml missing 'runs' field"
            exit 1
          fi
          
          echo "✅ action.yml validation passed"
      
      - name: 📊 Check Action Inputs
        run: |
          required_inputs=("openai-api-key" "staging-url" "staging-email" "staging-password" "target-repo-path" "git-user-name" "git-user-email" "test-directory")
          
          for input in "${required_inputs[@]}"; do
            if ! grep -q "$input:" action.yml; then
              echo "❌ Missing required input: $input"
              exit 1
            fi
          done
          
          echo "✅ All required inputs present"